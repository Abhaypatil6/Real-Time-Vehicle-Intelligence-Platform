# Real-Time-Vehicle-Intelligence-Platform
I have built a real-time ML inference platform using FastAPI where a trained model is deployed as an API to provide low-latency predictions, and I containerized and deployed it for scalable usage.
